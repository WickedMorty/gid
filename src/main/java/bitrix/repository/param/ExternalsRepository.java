package bitrix.repository.param;

import bitrix.entity.param.Externals;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ExternalsRepository extends JpaRepository<Externals, Integer> {
}
